include ../includes/mixins.pug
doctype html
block vars
html(lang="ru-RU")
    head
        meta(charset="UTF-8")
        meta(name="viewport" , content="width=device-width, initial-scale=1")
        meta(http-equiv="X-UA-Compatible" content="IE=edge")
        title #{title}
        include ../includes/links.pug
    body
        include ../includes/svg-sprite
        .g-app(style="background-image: url('img/Background.png')")
            block header
            .g-wrapper
                .g-side
                .g-content
                    block content
                .g-side.g-side--hide

        include ../includes/modals/auth-modal
        script(src="js/common.js")
